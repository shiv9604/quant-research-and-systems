# programme for greeting someone 

def greet(name): # user is the parameter in the function
    return print("Good Morning, " + name)
user = input("Enter your name : ")
# print(greet(user))
greet(user)