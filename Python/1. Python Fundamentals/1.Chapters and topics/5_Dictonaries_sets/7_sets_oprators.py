# Lenth of set
a = {1,2,3,4,5}
print(a,len(a))

#Remove method
a.remove(4) # removes that perticular value (value)
print(a)

#Set pop
print(a.pop()) # --> removes any random value

#Set Clear
print(a.clear) # --> clears the set

#set union
a.union() # --> returns the new set contaning the union values of set

# Set intersection
a.intersection() # --> returns the new set only matching values in the sets.5_Dictonaries_sets