// Local storage in JavaScript (which stores the data in clients computer in the form of string)

// Never store the client's sensitive data in local storage Bcoz it can be viewable in the (inspect/application/localstorage)

// setItem => (Set values to local storage)
localStorage.setItem('Name',"Shivprasad Madhav Kounsalye");

// getItem => (Get the value of that item)
localStorage.getItem('Name');